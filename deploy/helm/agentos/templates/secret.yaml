{{- if not .Values.vault.existingSecret }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "agentos.fullname" . }}-vault
type: Opaque
stringData:
  AGENTOS_VAULT_PASSPHRASE: {{ .Values.vault.passphrase | quote }}
{{- end }}
