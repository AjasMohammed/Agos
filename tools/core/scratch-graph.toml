tags = ["read", "fs"]

[manifest]
name        = "scratch-graph"
version     = "1.0.0"
description = "Get the local subgraph around a scratchpad page via BFS traversal"
author      = "agentos-core"
trust_tier  = "core"

[capabilities_required]
permissions = ["scratchpad:r"]

[capabilities_provided]
outputs = ["content.text"]

[intent_schema]
input  = "ScratchGraphIntent"
output = "ScratchGraphResult"

[payload_schema]
type = "object"
required = ["title"]

[payload_schema.properties.title]
type = "string"
description = "Center page title for graph traversal"

[payload_schema.properties.depth]
type = "integer"
default = 2
description = "BFS traversal depth (max 5)"

[payload_schema.properties.max_pages]
type = "integer"
default = 10
description = "Maximum pages to include in the subgraph (max 50)"

[sandbox]
network       = false
fs_write      = false
gpu           = false
max_memory_mb = 64
max_cpu_ms    = 15000
syscalls      = []
