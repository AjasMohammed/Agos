tags = ["read"]
risk_class = "readonly_scoped"

[manifest]
name        = "agent-list"
version     = "1.0.0"
description = "List all registered agents with their status. Optional 'status' filter (online/idle/offline). Use to discover available peers before sending agent messages."
author      = "agentos-core"
trust_tier  = "core"
category    = "agent"
search_hints = ["which agents exist", "who is online", "list peers"]

[capabilities_required]
permissions = ["agent.registry:r"]

[capabilities_provided]
outputs = ["content.structured"]

[intent_schema]
input  = "AgentListQuery"
output = "AgentListResult"

[payload_schema]
type = "object"

[payload_schema.properties.status]
type = "string"
description = "Optional agent status filter (case-insensitive), e.g. idle or busy"

[sandbox]
network       = false
fs_write      = false
gpu           = false
max_memory_mb = 16
max_cpu_ms    = 1000
syscalls      = []
