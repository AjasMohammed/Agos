tags = ["exec"]
risk_class = "control_plane"

[manifest]
name        = "task-delegate"
version     = "1.0.0"
description = "Delegate a sub-task to a named agent. The child runs in the background: keep working, its output is delivered into your context when it finishes. If you finish first, you are paused and resumed automatically when the child reports — no polling needed."
author      = "agentos-core"
trust_tier  = "core"
category    = "agent"
search_hints = ["hand this task to another agent", "delegate a task"]

[capabilities_required]
permissions = ["agent.message:x"]

[capabilities_provided]
outputs = ["status"]

[intent_schema]
input  = "TaskDelegateIntent"
output = "DelegateResult"

[payload_schema]
type = "object"
required = ["agent", "task"]

[payload_schema.properties.agent]
type = "string"
description = "Target agent name"

[payload_schema.properties.task]
type = "string"
description = "Sub-task prompt for the target agent"

[payload_schema.properties.priority]
type = "integer"
default = 5
minimum = 0
maximum = 255
description = "Delegated task priority"

[sandbox]
network       = false
fs_write      = false
gpu           = false
max_memory_mb = 16
max_cpu_ms    = 1000
syscalls      = []
