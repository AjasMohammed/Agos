Read every plan file in docs/plans/. For each plan, read the corresponding implementation files referenced in the plan. Compare what the plan specifies vs what's actually implemented. For each gap, classify it as: missing_feature, partial_implementation, or spec_drift. Generate an updated status file at docs/plans/audit_report.md with: 1) a table showing each plan's completion percentage, 2) a prioritized list of all gaps sorted by severity, 3) for each gap, the exact files that need changes and what changes are needed. Then create individual TODO plan files for any phase below 90% completion.